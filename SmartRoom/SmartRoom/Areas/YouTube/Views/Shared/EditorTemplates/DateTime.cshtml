@*@model DateTime?*@

@inherits System.Web.Mvc.WebViewPage<System.DateTime?>

@{
    var htmlAttributes = ViewData["htmlAttributes"] ?? new { };
}

        @Html.TextBox("", string.Format("{0:MM-DD-yyyy HH:mm}", Model.HasValue ? Model : null), htmlAttributes)

     

