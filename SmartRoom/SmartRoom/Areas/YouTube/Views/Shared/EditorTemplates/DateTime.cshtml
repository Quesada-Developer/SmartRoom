@*@model DateTime?*@

@inherits System.Web.Mvc.WebViewPage<System.DateTime?>

@{
    var htmlAttributes = ViewData["htmlAttributes"] ?? new { };
}

@Html.TextBox("", string.Format("{0:yyyy-MM-dd HH:mm}", Model.HasValue ? Model : null), htmlAttributes)